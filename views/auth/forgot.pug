extends ../layout/header.pug
block content
    div(class="relative w-full lg:w-1/2 min-h-[300px] lg:min-h-screen p-4 sm:p-8 overflow-hidden bg-gray-800 bg-opacity-50 flex items-center justify-center z-10")
        div(class="absolute inset-0 grid grid-cols-3 gap-2 opacity-30 z-0")
            each i in Array(12)
                div(class="bg-gray-900 rounded-xl aspect-square m-1")
        
        // Contenedor de las tarjetas principales (centrado sobre el fondo)
        div(class="relative z-10 grid grid-cols-2 gap-4 max-w-md w-full")
            // Tarjeta 1: Total Care. Total Different.
            div(class="bg-purple-600 text-white p-4 sm:p-6 rounded-2xl shadow-xl flex flex-col justify-between aspect-square col-span-1 h-full")
                // Patrón de cruces
                div(class="h-4 w-full flex flex-wrap gap-1 mb-2")
                    each j in Array(20)
                        div(class="h-1 w-1 bg-purple-300 opacity-70 rounded-full transform rotate-45")

                p(class="text-xl sm:text-2xl font-semibold") Total Care.
                p(class="text-xl sm:text-2xl font-semibold") Total Different.

            // Tarjeta 2: Ícono
            div(class="bg-purple-600 text-white p-6 rounded-2xl shadow-xl flex items-center justify-center aspect-square col-span-1 h-full")
                // Símbolo de trueno (reemplazo simple con SVG o texto grande)
                div(class="text-7xl sm:text-8xl font-bold transform rotate-12 leading-none") ⚡

            // Tarjeta 3: Building trust...
            div(class="bg-yellow-400 text-gray-900 p-4 sm:p-6 rounded-2xl shadow-xl flex flex-col justify-between aspect-square col-span-1 h-full")
                div(class="text-4xl font-light") +
                p(class="text-base sm:text-lg font-semibold") Building trust in blockchain technology

            // Tarjeta 4: Own your power
            div(class="bg-yellow-400 text-gray-900 p-4 sm:p-6 rounded-2xl shadow-xl flex flex-col justify-between aspect-square col-span-1 h-full")
                div(class="text-4xl font-light") +
                p(class="text-base sm:text-lg font-semibold") Own your power
        
    
    // === Bloque del Formulario (Sección Derecha) ===//
    div(class="w-full lg:w-1/2 flex items-center justify-center p-4 sm:p-8 lg:py-20 xl:py-32 z-20")
        div(class="max-w-md w-full")
            h2(class="text-3xl sm:text-4xl font-semibold mb-2")=titulo
            p(class="text-gray-400 text-sm mb-8") Reestablece la contraseña

            form(class="space-y-4"  method="POST" novalidate)
                input(type="hidden", name="_csrf" value = csrfToken)
            
                if errores
                    each error in errores
                        div(class="flex items-center max-w-sm w-full bg-red-50 border border-red-400 text-red-800 px-4 py-3 rounded-2xl shadow-lg animate-slideIn")
                            svg(class="w-5 h-5 mr-2 text-red-500", xmlns="http://www.w3.org/2000/svg", fill="none", viewBox="0 0 24 24", stroke="currentColor")
                            path(stroke-linecap="round", stroke-linejoin="round", stroke-width="2", d="M6 18L18 6M6 6l12 12")
                            span(class="text-sm font-medium")=error.msg

                if success
                    each exito in success
                        div(class="flex items-center max-w-sm w-full bg-green-50 border border-green-400 text-green-800 px-4 py-3 rounded-2xl shadow-lg animate-slideIn")
                            svg(class="w-5 h-5 mr-2 text-green-500", xmlns="http://www.w3.org/2000/svg", fill="none", viewBox="0 0 24 24", stroke="currentColor")
                            path(stroke-linecap="round", stroke-linejoin="round", stroke-width="2", d="M6 18L18 6M6 6l12 12")
                            span(class="text-sm font-medium")=exito.msg
                //fin toast
                div
                label(class="block text-sm font-medium text-gray-400" for="email") Escribe el email con que te registraste
                    input#email(class="mt-1 block w-full px-4 py-3 bg-gray-800 border border-gray-700 rounded-lg text-white focus:ring-purple-500 focus:border-purple-500" type="email" name="email" placeholder="miemail@email.com" required)
            
                
                button(class="w-full py-3 mt-6 bg-white text-gray-900 font-bold rounded-full shadow-lg hover:bg-gray-200 transition duration-200 cursor-pointer" type="submit") Recuperar

                div(class="flex items-center my-6")
                    div(class="flex-grow border-t border-gray-700")
                    span(class="mx-4 text-gray-500") O
                    div(class="flex-grow border-t border-gray-700")
                
                div(class="flex justify-between space-x-4")
                    a(class="w-1/2 py-3 text-center text-sm font-medium text-gray-300 bg-gray-800 rounded-lg hover:text-white hover:bg-gray-700 transition duration-200" href="/admin/register") Crear cuenta

                
                    a(class="w-1/2 py-3 text-center text-sm font-medium text-gray-300 bg-gray-800 rounded-lg hover:text-white hover:bg-gray-700 transition duration-200" href="/admin") Login