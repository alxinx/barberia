div
  h2(class="text-lg font-semibold text-slate-800 mb-6") Información de la Barbería
      //toast

      if errores
          each error in errores
              div(class="flex items-center max-w-sm w-full bg-red-50 border border-red-400 text-red-800 px-4 py-3 rounded-2xl shadow-lg animate-slideIn")
                  svg(class="w-5 h-5 mr-2 text-red-500", xmlns="http://www.w3.org/2000/svg", fill="none", viewBox="0 0 24 24", stroke="currentColor")
                  path(stroke-linecap="round", stroke-linejoin="round", stroke-width="2", d="M6 18L18 6M6 6l12 12")
                  span(class="text-sm font-medium")=error.msg


      if success
          each exito in success
              div(class="flex items-center max-w-sm w-full bg-green-50 border border-green-400 text-green-800 px-4 py-3 rounded-2xl shadow-lg animate-slideIn")
                  svg(class="w-5 h-5 mr-2 text-green-500", xmlns="http://www.w3.org/2000/svg", fill="none", viewBox="0 0 24 24", stroke="currentColor")
                  path(stroke-linecap="round", stroke-linejoin="round", stroke-width="2", d="M6 18L18 6M6 6l12 12")
                  span(class="text-sm font-medium")=exito.msg
    


  //End Toast


  form( method="POST" enctype="multipart/form-data" class="space-y-6")
    input(type="hidden", name="_csrf" value = csrfToken)
    input(type="hidden", name="idPuntoVenta" value  = (datosPunto ? datosPunto.idPuntoVenta : ""))
    
    //- Nombre Barbería
    div(class="grid grid-cols-1 md:grid-cols-3 gap-4")
        div
         label(for="nombre" class="block text-sm font-medium text-slate-700 mb-1") Nombre Barbería
            input(
            type="text"
            id="nombre"
            name="nombre"
            placeholder="Barbería Central"
            value = datosPunto ? datosPunto.nombre : ""
            class="block w-full rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm text-slate-800 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
            )
            if camposFallidos 
                each fallido in camposFallidos
                  if fallido == 'nombre'
                    span(class = "flex items-center max-w-sm w-full bg-red-50 border border-red-400 text-red-800 px-4 py-1 rounded-b-sm shadow-lg animate-slideIn w-4xl") 
                     p Necesito el nombre

            
        div
         label(for="direccion" class="block text-sm font-medium text-slate-700 mb-1") Dirección Barbería
            input(
            type="text"
            id="direccion"
            name="direccion"
            placeholder="Cra 10 # 20-30"
            value = datosPunto ? datosPunto.direccion : ""
            class="block w-full rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm text-slate-800 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
            )
            if camposFallidos 
                each fallido in camposFallidos
                  if fallido == 'direccion'
                    span(class = "flex items-center max-w-sm w-full bg-red-50 border border-red-400 text-red-800 px-4 py-1 rounded-b-sm shadow-lg animate-slideIn w-4xl") 
                     p Debes darme la dirección
            
        div
         label(for="WhatsApp" class="block text-sm font-medium text-slate-700 mb-1") WhatsApp Barbería
            input(
            type="phone"
            id="WhatsApp"
            name="WhatsApp"
            placeholder="+57 300 000 0000"
            value = datosPunto ? datosPunto.WhatsApp : ""
            class="block w-full rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm text-slate-800 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
            )
            if camposFallidos 
                each fallido in camposFallidos
                  if fallido == 'WhatsApp'
                    span(class = "flex items-center max-w-sm w-full bg-red-50 border border-red-400 text-red-800 px-4 py-1 rounded-b-sm shadow-lg animate-slideIn w-4xl") 
                     p Necesito el número de WhatsApp
        
    //- Ciudad + Capacidad
    div(class="grid grid-cols-1 md:grid-cols-4 gap-4")
      div
        label(for="ciudad" class="block text-sm font-medium text-slate-700 mb-1") Ciudad
        input(
          type="text"
          id="ciudad"
          name="ciudad"
          placeholder="Medellín",
          value = datosPunto ? datosPunto.ciudad : ""
          class="block w-full rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm text-slate-800 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
        )
        if camposFallidos 
                each fallido in camposFallidos
                  if fallido == 'ciudad'
                    span(class = "flex items-center max-w-sm w-full bg-red-50 border border-red-400 text-red-800 px-4 py-1 rounded-b-sm shadow-lg animate-slideIn w-4xl") 
                     p En cuál ciudad está la Barbería?
            




      div
        label(for="capacidad" class="block text-sm font-medium text-slate-700 mb-1") Capacidad
        input(
          type="number"
          id="capacidad"
          name="capacidad"
          min="1"
          placeholder="10"
          value = datosPunto ? datosPunto.capacidad : ""
          class="block w-full rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm text-slate-800 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
        )
        if camposFallidos 
                each fallido in camposFallidos
                  if fallido == 'capacidad'
                    span(class = "flex items-center max-w-sm w-full bg-red-50 border border-red-400 text-red-800 px-4 py-1 rounded-b-sm shadow-lg animate-slideIn w-4xl") 
                     p Cuántos barberos pueden albergar?

          
      div
        label(for="nit" class="block text-sm font-medium text-slate-700 mb-1") NIT
        input(
          type="text"
          id="nit"
          name="nit"
          value = datosPunto ? datosPunto.nit : ""
          placeholder="900.123.456-7"
          class="block w-full rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm text-slate-800 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
        )
        if camposFallidos 
                each fallido in camposFallidos
                  if fallido == 'nit'
                    span(class = "flex items-center max-w-sm w-full bg-red-50 border border-red-400 text-red-800 px-4 py-1 rounded-b-sm shadow-lg animate-slideIn w-4xl") 
                     p Es importante el NIT
            

      div
        label(for="razonSocial" class="block text-sm font-medium text-slate-700 mb-1") Razón Social 
        input(
          type="text"
          id="razonSocial"
          name="razonSocial"
          value = datosPunto ? datosPunto.razonSocial : ""
          placeholder="Barbería Central S.A.S."
          class="block w-full rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm text-slate-800 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
        )
        if camposFallidos 
                each fallido in camposFallidos
                  if fallido == 'razonSocial'
                    span(class = "flex items-center max-w-sm w-full bg-red-50 bordeborder-red-400 text-red-800 px-4 py-1 rounded-b-sm shadow-lg animate-slideIn w-4xl") 
                     p Cuál es la razón social?
        

      
    hr(class="border-solid border-amber-400")
    //- Email + Contraseña
    div(class="grid grid-cols-1 md:grid-cols-3 gap-4")
      div
        label(for="email" class="block text-sm font-medium text-slate-700 mb-1") Email
        input(
          type="email"
          id="email"
          name="email"
          value = datosPunto ? datosPunto.email : ""
          placeholder="admin@barberia.com"
          class="block w-full rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm text-slate-800 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
        )

        if camposFallidos 
                each fallido in camposFallidos
                  if fallido == 'email'
                    span(class = "flex items-center max-w-sm w-full bg-red-50 bordeborder-red-400 text-red-800 px-4 py-1 rounded-b-sm shadow-lg animate-slideIw-4xl") 
                     p Necesito un email
      div
        label(for="password" class="block text-sm font-medium text-slate-700 mb-1") Contraseña
        input(
          type="password"
          id="password"
          name="password"
          placeholder="••••••••"
          class="block w-full rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm text-slate-800 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
        )
        if camposFallidos 
                each fallido in camposFallidos
                  if fallido == 'password'
                    span(class = "flex items-center max-w-sm w-full bg-red-50 border border-red-400 text-red-800 px-4 py-1 rounded-b-sm shadow-lg animate-slideIn w-4xl") 
                     p Debes darme una contraseña de mas de 4 caracteres.
      div
        label(for="password_again" class="block text-sm font-medium text-slate-700 mb-1") Dame de nuevo la contraseña 
        input(
          type="password"
          id="password_again"
          name="password_again"
          placeholder="••••••••"
          class="block w-full rounded-lg border border-gray-200 bg-gray-50 px-3 py-2.5 text-sm text-slate-800 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-indigo-500"
        )
        if camposFallidos 
                each fallido in camposFallidos
                  if fallido == 'password_again'
                    span(class = "flex items-center max-w-sm w-full bg-red-50 border border-red-400 text-red-800 px-4 py-1 rounded-b-sm shadow-lg animate-slideIn w-4xl") 
                     p Las contraseñas no coinciden

        

    //- Upload foto
    div
      label(for="foto" class="block text-sm font-medium text-slate-700 mb-1") Foto de la barbería
      div(
        class="mt-1 flex items-center justify-center rounded-xl border-2 border-dashed border-gray-200 bg-gray-50 px-4 py-6 cursor-pointer hover:border-indigo-400 hover:bg-indigo-50 transition"
        onclick="document.getElementById('foto').click()"
      )
        div(class="text-center text-xs text-gray-500")
          span(class="block font-medium text-slate-700 mb-1") Haz clic para subir o arrastra una imagen
          span(class="block") JPG o PNG, máximo 2MB
      input(type="file" id="foto" name="foto" accept="image/*" class="hidden")


    div(class="flex justify-center")
        button(
            type="submit"
            class="px-5 py-2 w-4xl rounded-lg text-sm font-medium text-white bg-green-500 hover:bg-green-600 shadow-md transition cursor-pointer"
            )=btn
            

    #mapa.h-96
